[project]
name = "venturalitica-scenario-medical"
version = "0.5.0"
description = "Spine segmentation scenario: base ML vs governance-instrumented comparison"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
python-version = "3.10"
